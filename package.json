{
  "name": "personal-site",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "serve": "node dev.js --serve-only",
    "dev": "node dev.js"
  },
  "dependencies": {
    "marked": "^15.0.7"
  }
}
